#include <unistd.h>
#include "main.h"

/**
 * add- Entry point
 *
 * @x: first parameter
 * @y: second parameter
 *
 * Description: Add two number
 *
 * Return: Always sum (success)
 */
int add(int x, int y)
{
	return (x + y);
}
